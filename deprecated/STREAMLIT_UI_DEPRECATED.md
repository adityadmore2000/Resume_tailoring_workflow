# Streamlit UI (Deprecated)

As of the Next.js migration, Streamlit is no longer the supported product UI.

- Frontend UI: `frontend/` (Next.js)
- Backend API: `backend/` (FastAPI)

The old Streamlit pages/entrypoints were kept in this folder only for reference during transition.

